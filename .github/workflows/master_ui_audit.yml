name: 🏆 Master UI & Chaos Audit

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  ui-chaos-certification:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: 🐍 Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 🖥️ Installa Monitor Virtuale (Xvfb & Dipendenze Qt)
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libxkbcommon-x11-0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-xinerama0 libxcb-xfixes0 libxcb-shape0 libegl1 libgl1 libxcb-cursor0

      - name: 📦 Installa Dipendenze Python
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-qt psutil
          playwright install chromium

      - name: 🎨 1. MASTER UI TEST (Test Interfaccia con Pytest-Qt)
        run: PYTHONPATH=${{ github.workspace }} xvfb-run --auto-servernum python -m pytest tests/tests/ui/MASTER_UI_TEST.py -v

      - name: 🔥 2. REAL ATTACK TEST (Chaos Engineering)
        run: PYTHONPATH=${{ github.workspace }} python tests/tests/system_integrity/REAL_ATTACK_TEST.py

      - name: 🧠 3. MINI-SOAK TEST (Stress Test RAM)
        run: PYTHONPATH=${{ github.workspace }} python tests/tests/system_integrity/SOAK_24H_TEST.py

      - name: 👑 4. GOD MODE V2 (Hedge Fund Chaos Engine)
        run: PYTHONPATH=${{ github.workspace }} xvfb-run -a python -u tests/stress_lab/GOD_MODE_V2_chaos.py
