name: 🕵️‍♂️ Anti-Detect Security Check (Ghost Mode)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1'

jobs:
  stealth-audit:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout del codice repository
      uses: actions/checkout@v4
      
    - name: Setup Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
        
    - name: Installazione Dipendenze
      run: |
        python -m pip install --upgrade pip
        pip install playwright pytest requests
        
    - name: Installazione Motore Chromium C++
      run: python -m playwright install chromium --with-deps
      
    - name: 🕵️‍♂️ Esecuzione Ultimate Anti-Detect Audit
      env:
        # Passiamo il Token Webshare (Assicurati che la secret esista e sia corretta!)
        WEBSHARE_TOKEN: ${{ secrets.WEBSHARE_TOKEN }}
      run: |
        echo "🔄 Interrogazione API Webshare in corso..."
        
        # Micro-script Python corazzato per estrarre l'IP IT
        export PROXY_URL=$(python -c "
        import os, sys, requests
        token = os.getenv('WEBSHARE_TOKEN')
        
        if not token:
            print('❌ ERRORE: WEBSHARE_TOKEN è vuoto. Controlla i Secrets su GitHub!', file=sys.stderr)
            sys.exit(0)
            
        try:
            url = 'https://proxy.webshare.io/api/v2/proxy/list/?page=1&page_size=100'
            headers = {'Authorization': f'Token {token}'}
            r = requests.get(url, headers=headers, timeout=15)
            
            if r.status_code != 200:
                print(f'❌ ERRORE API WEBSHARE (HTTP {r.status_code}): {r.text}', file=sys.stderr)
                sys.exit(0)
                
            proxies = r.json().get('results', [])
            it_proxies = [x for x in proxies if x.get('country_code') == 'IT' and x.get('valid')]
            
            if not it_proxies:
                print('❌ ERRORE: Webshare ha risposto OK, ma non ci sono proxy IT validi.', file=sys.stderr)
                sys.exit(0)
                
            p = it_proxies[0]
            # Output finale per la bash
            print(f\"http://{p['username']}:{p['password']}@{p['proxy_address']}:{p['port']}\")
            
        except Exception as e:
            print(f'❌ ERRORE DI RETE PYTHON: {str(e)}', file=sys.stderr)
        ")

        if [ -z "$PROXY_URL" ]; then
            echo "⚠️ Attenzione: Nessun proxy recuperato. Guarda l'errore ❌ qui sopra nei log."
        else
            # Nascondiamo la password per sicurezza nei log di GitHub
            MASKED_URL=$(echo "$PROXY_URL" | sed -e 's/\(http:\/\/[^:]*:\)[^@]*\(@.*\)/\1***\2/')
            echo "✅ Proxy Italiano estratto con successo: $MASKED_URL"
        fi

        echo "🚀 Avvio dell'Audit Anti-Detect..."
        
        # xvfb-run emula un display X11 virtuale (fondamentale per ingannare i test anti-bot)
        # PROXY_URL è già stato esportato nell'ambiente, quindi il tuo script Python lo leggerà
        xvfb-run -a python -u tests/tests/system_integrity/ANTI_DETECT_AUDIT.py
