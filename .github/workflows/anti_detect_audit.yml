name: 🕵️‍♂️ Anti-Detect Security Check (Ghost Mode)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: # Permette di avviarlo manualmente dal sito
  schedule:
    # Esegue l'audit in automatico ogni Lunedì alle 04:00 AM (UTC)
    - cron: '0 4 * * 1'

jobs:
  stealth-audit:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout del codice repository
      uses: actions/checkout@v4
      
    - name: Setup Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
        
    - name: Installazione Dipendenze (Playwright)
      run: |
        python -m pip install --upgrade pip
        pip install playwright pytest
        
    - name: Installazione Motore Chromium C++
      run: python -m playwright install chromium --with-deps
      
    - name: 🕵️‍♂️ Esecuzione Ultimate Anti-Detect Audit
      env:
        # Inietta il proxy dai segreti di GitHub (se configurato)
        # Formato: http://username:password@ip:porta
        PROXY_URL: ${{ secrets.PROXY_URL }}
      run: |
        # xvfb-run emula un display X11 virtuale (fondamentale per ingannare
        # i test anti-bot che cercano la presenza di uno schermo fisico)
        # Il flag -u forza i log in tempo reale su GitHub!
        xvfb-run -a python -u tests/tests/system_integrity/ANTI_DETECT_AUDIT.py
